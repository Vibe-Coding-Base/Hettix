query LLMSettings {
  llmSettings {
    provider
    baseURL
    model
    hasApiKey
    enabled
  }
}

mutation UpdateLLMSettings($input: UpdateLLMSettingsInput!) {
  updateLLMSettings(input: $input) {
    provider
    baseURL
    model
    hasApiKey
    enabled
  }
}
