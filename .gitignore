*.vscode
/dist
*.exe~
/pkg/adminui/admin
*.pem
*.db
*.sqlite
*.test

# All build artifacts are centralised here (binaries, desktop apps, installer).
/releases/

# Fetched portable browser (never committed).
/browser/

# Wails writes intermediate output here; the final app is copied into /releases.
# The auto-generated JS runtime lives here too (the admin UI is in /admin).
/cmd/hettix-desktop/build/bin/
/cmd/hettix-desktop/frontend/
